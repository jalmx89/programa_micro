#include <entradas.h>

#use fast_io(A)   //configuro el puerto A
#use fast_io(B)   //configuro el puerto B

void main()
{

   set_tris_a(255); //tengo que configurar como entrada
   
   while(TRUE)
   {
      //TODO: User Code
   }

}
