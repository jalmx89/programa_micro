#include <Multi.h>

#use fast_io(A)
#use fast_io(B)

void main()
{
   set_tris_a(255);
   set_tris_b(0);
   
   output_a(0);
   output_b(0);
   
   while(TRUE)
   {
      //TODO: User Code
      if(input_a(PIN_A0) == 1){
      
      }
   }

}
